- type: shader
  id: DisplacedStencilDraw
  kind: source
  path: "/Textures/Shaders/displacement.swsl"
  # DeadRot Naked Fix Start
  # stencil:
  #   ref: 1
  #   op: Keep
  #   func: NotEqual
  # DeadRot Naked Fix End
  params:
    displacementSize: 127
